let fruit = [];
let browsers = ["Chrome", "Firefox", "Safari", "Edge", "Opera"];

console.log(browsers[0]); // Output: Chrome
console.log(browsers[2]); // Output: Safari
console.log(browsers[4]); // Output: Opera
console.log(browsers.length); // Output: 5


//methods of array
push(x); //adde element at last
pop(x); //remove last element
unshift(X); //add element at first
shift(x); //remove the first element    
splice(i, n, ...items); //remove element from the middle or place we want
sort(); //sort the array
reverse(); //reverse the array

