// undefined - A variable exists, but it has not been assigned a vlue yet. JS itself sets this automatically

// null - A variable exists, but it has been explicitly set to have no value. This is done by the programmer

// datatype of undefined is undefined
// datatype of null is object

var x;
console.log(x); // Output: undefined

var audi = null;
console.log(audi); // Output: null